
. oggetti che passo al client tramite gli eventi devono essere cloni? Cloni mutabili.

- come gestire risposta evento lato server? -> con ackResponse nel listener che avrebbe la priorità sulla coda?
  Poi lato client si aspetta l'ackResponse per ripetere o passare al prossimo OK

- test su tutto.

- per individuare disconnessioni: thread con server che pinga client ogni tot OK

- parametro riconnessione client: nickname, token (password)

- per persistenza in caso di crash del server: sarebbe da salvare ogni tot tutto lo stato del gioco in un file ? file Json o altro?
    -dump dello stato di gioco
        -oppure serializzare in Json (oppure binario, xml..) il model e fare un costruttore che ricarica le classi di Json
            -check transient attributes (non devono essere serializzate perché cambiano, in caso di crash)

- consiglio su GUI (swing)

- per GUI: servono eventi per passare oggetti grafici o si avranno a disposizione sul client?

- In serverstub e clientskeleton quando fai update devi passare anche il Client?? è necessario sapere a chi devo passare gli update!!
-Canali di comunicazione tra client e server: 4 canali diversi per ogni client che convergono in uno unico per il server?  è diramato oppure una cosa unica?

                                                           SERVER
                                                            | |
                                                            | |
                                                            / \
                                                           /   \
                                                          /     \
                                                         /       \
                                                        /         \
                                                       /   /| |\   \
                                                      /   / | | \   \
                                                     CL_1  CL_2   CL_3




//TODO
- aggiungere eventi mancanti: EndTurn per tabellone ogni fine turno, StartGame per situazione iniziale,place starting card, return play card
- cambiare eventi :returnDrawnCard, play card request aggiunte current resources ✅
- passare oggetti come cloni negli eventi ✅
- sistemare handle event con  un thread per mandare ackresponse✅
- ackresponse se positiva viene mandata a tutti✅
- gestione ackresponse in tui ✅
- password associata a ogni nickname per riconnessione 🔜
- modificare il set del nickname: aggiungere il numero progressivo in fondo al nickname a tutti ✅
  -> bisogna mostrare quindi il vero nickname ai giocatori
- comando per ruotare carta in tui per eventi play card e place starting card ✅
- objective deck da sistemare!

- logica per riconnessione
- vedere se riusciamo a far funzionare qualcosa per la prossima volta
- finire test su model, incominciare test controller?
- incominciare gui?


Domande
- model e controller devono girare su thread separati? e il server invece si può accoppiare con uno dei due?

DEBUG 10/05:
    11/05 00:42 Siamo morti, distrutti...non ce la facciamo più. I problemi sono:
        Server non riesce a mandare gli eventi ma il client sì (boh) (in socket)
        mvListener sembra essere null con l'instanziazione due righe sopra (booh)
        Continuare a debuggare da Controller.addPlayerToLobby