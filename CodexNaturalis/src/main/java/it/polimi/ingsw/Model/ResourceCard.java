package it.polimi.ingsw.Model;

/**
 * Resource Card represents the resource cards.
 * It implements PlayableCard because it uses all method and attribute of that class, which in turn extends Cards where all main features of resource card are declared.
 */
public class ResourceCard extends PlayableCard{

}
